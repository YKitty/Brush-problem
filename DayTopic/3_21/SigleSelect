1. 交换两个变量的时候可以直接使用+-，即使溢出了之后，最后交换的结果也应该是正确的    
这是因为相加的时候产生了一次上溢，但是在进行两次相加的时候发生了两次下溢，所以交换之后的结果是正确的    
2. 对于double，float类型的数据来说，不能直接使用==0这种方式来判断是否为0，因为其在内存中的存储方式是不一样的    
float：1符号位    8指数     23尾数
double：1符号位   11指数    52尾数
3. std::sort是一个不稳定的排序
4. std::bitset不是一个STL容器
5. 调用默认的构造函数的时候是不需要添加括号的，否则的话就是声明了一个函数   

