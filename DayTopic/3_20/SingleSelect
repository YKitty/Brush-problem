2. 数组的执行效率那个最高
  一般的累加的话，执行的效率就是最高的了
3. 堆和栈的区别 
  错误的是，堆只能动态开辟而不可以进行静态开辟
4. 对于静态变量有着自己的作用域范围，只是改变了该变量的生命周期
  
7. 对于创建对象来说，new出来的对象是存放在堆上的，不算做创建出来的对象

10. 对于delete的时候，使用的是哪个类型指针，访问的就是那个类型的对象，不会访问到他的子类中的方法，这是因为他只能看到自己类的成员方法，而看不到子类的成员方法


